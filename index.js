// variables for game state
let player1Score = 0;
let player2Score = 0;
let player1Turn = true;

// Create variables to store references to the necessary DOM nodes

let x = document.getElementById("") // player 1 counter
let y = document.getElementById("") // player 2 counter 
let z = document.getElementById("") // Dice roll??